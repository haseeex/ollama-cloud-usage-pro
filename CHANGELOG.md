# 更新日志

Ollama Cloud 用量监控扩展的所有重要变更都会记录在此。

## 1.2.1 - 2026-09-21

### 变更
- 扩展更名为 **Ollama Cloud 用量监控 Pro**（ID：`ollama-cloud-usage-pro`），以区别于上游原扩展。
- 扩展图标改为与 Visual Studio 版一致的羊驼滑板车造型，配色由金色调整为蓝色。
- README 截图更新为状态栏悬浮面板、用量面板与设置项三张实拍图，并改用 GitHub 远程图片地址，确保扩展商店页面正常显示。

## 1.2.0 - 2026-09-21

### 新增
- **配额预测**：按当前窗口平均消耗估算重置前还能请求多少次，窗口级与单模型级均有。模型列表新增「占窗口比例」列（窗口用量 × 该模型请求占比）。所有预测值悬浮可查看计算依据。
- **用量显示精度**：新增 `ollamaCloud.usagePrecision` 设置项（0–4 位小数，默认 1）与 `Ollama Cloud: 设置用量显示精度` 命令。占比类数值在设定精度下会显示为 0 时自动提高精度至可见。
- **中英文界面**：新增 `ollamaCloud.language` 设置项（`auto`/`zh`/`en`，默认跟随 VS Code）与 `Ollama Cloud: 切换语言 (中/EN)` 命令。语言与精度切换即时生效，无需重新请求 API。
- **状态栏悬浮面板显示模型明细**：与 Visual Studio 版对齐，每个窗口区块现在包含标题行（用量 % + 剩余预测）、多模型堆叠用量条、重置倒计时，以及逐个模型的明细列表（色点、名称、占窗口比例、请求次数、剩余预测）。
- 用量条宽度从 18 格提升至 40 格，采用最大余数法分配并保证每个有请求的模型至少占 1 格，避免小用量模型从条上消失。
- 状态栏文案改为可配置精度显示（原先固定取整）。

### 变更
- 本地化字符串集中到 `localization.ts`，`api.ts` 的错误信息也随之支持中英文。
- 新增 `quotaPredictor.ts`（配额预测）与 `barLayout.ts`（用量条格分配与配色）及其单元测试，移植自 Visual Studio 版扩展。

## 1.1.0 - 2026-09-18

### 新增
- 可配置的自动刷新间隔：新增 `ollamaCloud.refreshInterval` 设置项（10–86400 秒，默认 60）、`Ollama Cloud: 设置自动刷新间隔` 命令，以及面板中的 ⚙ 按钮。
- 跨窗口请求协调：全局存储中的用量缓存由所有 VS Code 窗口共享。缓存新鲜时直接复用快照而不再请求；当多个窗口同时到期时，跨窗口锁确保只有一个窗口真正发起请求 —— 多开窗口不再成倍增加 API 请求。
- 面板底部显示当前刷新间隔与上次更新时间。
- 状态栏悬浮提示新增上次更新时间，与刷新频率一同展示。

### 变更
- 手动刷新（⟳ / `Ollama Cloud: 刷新用量`）现在会绕过共享缓存。
- 同一窗口内的并发刷新会被合并为单次请求。

### 修复
- 错误状态现在指向 `Ollama Cloud: 添加账户`，不再指向已移除的 `setApiKey` 命令。

## 1.0.5 - 2026-08-19

### 变更
- 重置窗口改为 UTC 对齐：5 小时窗口按 UTC 00/05/10/15/20 点边界重置，每周窗口按周一 UTC 00:00 重置。取代了此前基于本地时间 11:00 的推断方式。
- 状态栏：合并为单个图标，点击打开详情面板。移除了双击操作与独立的刷新按钮。
- 自动刷新间隔从 5 分钟缩短为 60 秒。
- 用量面板的进度条按模型拆分为多段，每段使用不同的蓝色色调（按索引分配）。移除了绿/黄/红的分级配色。
- 模型列表按 API 返回顺序排列；显示每个模型的请求次数（悬浮条形段可查看模型名与次数）。
- 悬浮提示改为配额表格，包含各模型占比条与重置倒计时。

### 修复
- 重置时间不再受本地时区 / 夏令时影响。

## 1.0.3 - 2026-08-04

### 修复
- 5 小时会话重置时间：改为基于当天 11:00 的滚动 5 小时窗口（与 Ollama 实际的重置行为一致），不再使用固定的整点时段。

## 1.0.2 - 2026-08-04

### 修复
- README 截图改用 GitHub 原始文件的绝对地址，确保在扩展市场与网页端都能正常显示。

## 1.0.1 - 2026-08-04

### 新增
- 在 README 中加入用量面板与状态栏的截图。

## 1.0.0 - 2026-08-04

### 新增
- 多账户支持：通过面板下拉框切换、添加、移除账户。
- 在每条用量条下方显示重置倒计时（5 小时窗口与每周窗口）。
- 状态栏指示器，按用量严重程度配色（绿/黄/红）。
- 每 5 分钟自动刷新。
- API 密钥安全存储在 VS Code Secret Storage 中。

## 0.0.1 - 初始版本

### 新增
- 查看 Ollama Cloud 的用量与限额。
